# Customer Mode UX Status

Date: 2026-09-10

The V1 customer composer now has first-class Text to Video, Image to Video, and AI Director presentation modes. The API client sends an explicit generation mode. The customer UI does not expose provider IDs or raw image URLs. Unicode prompt entry is intentional.

Verification is pending in the project environment because this execution environment cannot resolve GitHub for a local clone. Do not treat the customer typecheck/build as passed until it is executed normally.
